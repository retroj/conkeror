
// Conkeror does not use the global findBar itself, but does use the View
// Partial Source window, which uses the find bar.
//
// I just guessed at the values.. it would be nice to write our own View
// Source buffer anyway, and just get rid of the dialogs we get from mozilla.
//
// pref("accessibility.typeaheadfind.timeout",100);
pref("accessibility.typeaheadfind.flashBar",0);
// bool: "accessibility.typeaheadfind"
// bool: "accessibility.typeaheadfind.linksonly"
